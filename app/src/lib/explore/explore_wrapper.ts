
export interface ExploreWrapper {
    handle_mouse_move: (event: MouseEvent) => void;

    // eslint-disable-next-line
    display: any;
    description: string;
    primary_title: string;
    secondary_title: string;
}
